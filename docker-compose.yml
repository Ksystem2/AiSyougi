services:
  aisyougi-api:
    build:
      context: .
      dockerfile: backend/Dockerfile
    ports:
      - "8000:8000"
    environment:
      YANEURAOU_PATH: /app/engine/YaneuraOu
      YANEURAOU_CWD: /app/engine
